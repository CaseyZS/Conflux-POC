# Normalize line endings: store text as LF in the repo, check out native.
# Silences the "LF will be replaced by CRLF" warnings on Windows.
* text=auto

# Files that must stay LF regardless of platform.
*.sh text eol=lf
