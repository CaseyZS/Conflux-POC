# Copy to .env — these are the local-dev defaults; nothing here is secret.
DATABASE_URL="file:./prisma/dev.db"

# Session-cookie signing key for Auth.js. Generate your own:
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
AUTH_SECRET="replace-me"
